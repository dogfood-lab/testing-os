<?xml version="1.0" encoding="UTF-8"?>
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 880 480" role="img" aria-labelledby="verify-title verify-desc" preserveAspectRatio="xMidYMid meet">
  <title id="verify-title">Healthy npm run verify output</title>
  <desc id="verify-desc">Terminal capture of a successful npm run verify in the testing-os repo. The command sequence runs sync-version:check (clean, README block matches package.json version), check-doc-drift (21 of 21 checks passed), check-regression-pins (the Class #14 gate: declared @pins tags verified against a real AST, grandfathered migration debt named, zero orphans), build (tsc --build composite project, no errors), test:scripts (all script test files green), and the workspace test fan-out covering schemas, verify, ingest, findings, report, portfolio, and dogfood-swarm packages — every package reports passing tests with no failures and no warnings.</desc>

  <defs>
    <!-- Stage D amend: terminal-mock SVG keeps its dark terminal aesthetic in
         BOTH site themes — the `.term-bg` outer rect is intentionally
         transparent so the SVG no longer renders as a dark slab on a white
         handbook page in light mode. The inner `.term-frame` continues to
         carry the dark terminal chrome (this is a terminal mock — a light-
         terminal version would be visually dishonest). Operators in light
         mode see a self-contained dark terminal card centered on the page
         background, not a full-width dark slab. -->
    <style>
      .term-bg { fill: transparent; }
      .term-frame { fill: #18181b; stroke: #27272a; stroke-width: 1; rx: 8; }
      .term-bar { fill: #27272a; rx: 8 8 0 0; }
      .term-dot-r { fill: #f87171; }
      .term-dot-y { fill: #fbbf24; }
      .term-dot-g { fill: #34d399; }
      .term-title { fill: #d4d4d8; font-family: 'Inter', system-ui, sans-serif; font-size: 11px; }
      .mono { font-family: 'JetBrains Mono', ui-monospace, 'Cascadia Code', monospace; font-size: 12px; }
      .text-prompt { fill: #34d399; }
      .text-cmd { fill: #fafafa; }
      .text-ok { fill: #34d399; }
      .text-info { fill: #93c5fd; }
      .text-dim { fill: #a1a1aa; }
      .text-body { fill: #e4e4e7; }
    </style>
  </defs>

  <rect width="880" height="480" class="term-bg" />
  <rect x="20" y="20" width="840" height="440" class="term-frame" />
  <rect x="20" y="20" width="840" height="28" class="term-bar" />
  <circle cx="38" cy="34" r="6" class="term-dot-r" />
  <circle cx="58" cy="34" r="6" class="term-dot-y" />
  <circle cx="78" cy="34" r="6" class="term-dot-g" />
  <text x="440" y="38" text-anchor="middle" class="term-title">testing-os — npm run verify</text>

  <g class="mono">
    <text x="36" y="76"><tspan class="text-prompt">$</tspan> <tspan class="text-cmd">npm run verify</tspan></text>

    <text x="36" y="100" class="text-dim">&gt; testing-os@1.17.0 verify</text>
    <text x="36" y="116" class="text-dim">&gt; npm run sync-version:check &amp;&amp; npm run check-doc-drift &amp;&amp; npm run check-regression-pins &amp;&amp;</text>
    <text x="36" y="132" class="text-dim">    npm run build &amp;&amp; npm run test:scripts &amp;&amp; npm run test</text>

    <text x="36" y="164" class="text-info">[sync-version]</text><text x="160" y="164" class="text-body"> README version block matches package.json — clean.</text>

    <text x="36" y="186" class="text-info">[check-doc-drift]</text><text x="190" y="186" class="text-ok"> OK — 21 check(s) passed.</text>

    <text x="36" y="208" class="text-info">[check-regression-pins]</text><text x="266" y="208" class="text-ok"> declared @pins tags AST-verified, 0 orphans.</text>

    <text x="36" y="232" class="text-dim"># tsc --build (composite refs)</text>
    <text x="36" y="252" class="text-ok">  ✓ packages/schemas → built (all schemas, 0 errors)</text>

    <text x="36" y="280" class="text-dim"># node --test "scripts/*.test.mjs"</text>
    <text x="36" y="300" class="text-ok">  ✓ test:scripts — all script test files green</text>

    <text x="36" y="324" class="text-dim"># npm test --workspaces --if-present</text>
    <text x="36" y="344"><tspan class="text-ok">  ✓</tspan> <tspan class="text-body">@dogfood-lab/schemas         </tspan> <tspan class="text-dim">vitest</tspan>     <tspan class="text-ok">passed</tspan></text>
    <text x="36" y="360"><tspan class="text-ok">  ✓</tspan> <tspan class="text-body">@dogfood-lab/verify          </tspan> <tspan class="text-dim">node --test</tspan> <tspan class="text-ok">passed</tspan></text>
    <text x="36" y="376"><tspan class="text-ok">  ✓</tspan> <tspan class="text-body">@dogfood-lab/ingest          </tspan> <tspan class="text-dim">node --test</tspan> <tspan class="text-ok">passed</tspan></text>
    <text x="36" y="392"><tspan class="text-ok">  ✓</tspan> <tspan class="text-body">@dogfood-lab/findings        </tspan> <tspan class="text-dim">node --test</tspan> <tspan class="text-ok">passed</tspan></text>
    <text x="36" y="408"><tspan class="text-ok">  ✓</tspan> <tspan class="text-body">@dogfood-lab/report          </tspan> <tspan class="text-dim">node --test</tspan> <tspan class="text-ok">passed</tspan></text>
    <text x="36" y="424"><tspan class="text-ok">  ✓</tspan> <tspan class="text-body">@dogfood-lab/portfolio       </tspan> <tspan class="text-dim">node --test</tspan> <tspan class="text-ok">passed</tspan></text>
    <text x="36" y="440"><tspan class="text-ok">  ✓</tspan> <tspan class="text-body">@dogfood-lab/dogfood-swarm   </tspan> <tspan class="text-dim">node --test</tspan> <tspan class="text-ok">passed</tspan></text>
  </g>
</svg>
