summary: ""
boundaries:
  - name: .github
    globs:
      - .github/**
    role: config
  - name: assets
    globs:
      - assets/**
    role: config
  - name: atlas
    globs:
      - packages/atlas/**
    role: code
  - name: docker
    globs:
      - docker/**
    role: config
  - name: docs
    globs:
      - docs/**
    role: docs
  - name: dogfood
    globs:
      - dogfood/**
    role: config
  - name: dogfood-swarm
    globs:
      - packages/dogfood-swarm/**
    role: code
  - name: examples
    globs:
      - examples/**
    role: config
  - name: findings
    globs:
      - packages/findings/**
    role: code
  - name: fixtures
    globs:
      - fixtures/**
    role: code
  - name: indexes
    globs:
      - indexes/**
    role: config
  - name: ingest
    globs:
      - packages/ingest/**
    role: code
  - name: policies
    globs:
      - policies/**
    role: config
  - name: portfolio
    globs:
      - packages/portfolio/**
    role: code
  - name: records
    globs:
      - records/**
    role: config
  - name: report
    globs:
      - packages/report/**
    role: code
  - name: reports
    globs:
      - reports/**
    role: config
  - name: root
    globs:
      - "*"
    role: docs
  - name: schemas
    globs:
      - packages/schemas/**
    role: code
  - name: scripts
    globs:
      - scripts/**
    role: code
  - name: site
    globs:
      - site/**
    role: code
  - name: swarms
    globs:
      - swarms/**
    role: docs
  - name: verify
    globs:
      - packages/verify/**
    role: code
