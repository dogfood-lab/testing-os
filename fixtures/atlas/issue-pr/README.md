# issue-pr

An Atlas fixture for a job an issue starts that commits a patched file to a
branch and opens a pull request through actions/github-script, the shape
mcp-tool-registry's Operations has. The gate is said once, after all the
job does, so it limits the whole phrase.
