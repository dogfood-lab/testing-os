name: ci
on: [push]
jobs:
  integrity:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: npm ci
      - run: npm run build
      - run: node dist/cli.js manifest --check
      - run: ./dist/cli.js status
