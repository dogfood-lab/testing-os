# python-data

A package that reads its own bundled data directory through pathlib: a
directory beside the module, a file two directories up and back down, a
sibling found by with_name, and a joinpath.
