# unshipped-bins

An Atlas fixture for a crate's binary that nothing ships, the shape
commandui's console has. tool is built by CI with cargo build; pub-tool is
sent to crates.io by the publish workflow; console is in the workspace and
no workflow builds, installs, releases or publishes it.
