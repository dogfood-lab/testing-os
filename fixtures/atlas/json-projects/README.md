# json-projects

An Atlas fixture for a directory of projects kept as JSON, the shape
style-dataset-lab's `projects/` has: three projects of JSON records, one
of them with a script beside its data. `atlas init` proposes the directory
as data, not config: the one script does not make a part of twenty-one
JSON files code or configuration.
