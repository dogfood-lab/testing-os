# cargo-examples

An Atlas fixture for Cargo examples, the shape asset-forge has. The crate
has no binary; examples/export_all.rs is a program a person runs with cargo
run --example export_all, and examples/render/main.rs is one CI runs by
the same command. Each uses the crate's library.
