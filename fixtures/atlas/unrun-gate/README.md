# unrun-gate

An Atlas fixture for a gate script only people run, the shape plain-sight
has: `verify.sh` at the root runs the suite and more, while CI runs
pytest directly and never the script. The page says `verify.sh` runs in
no workflow.
