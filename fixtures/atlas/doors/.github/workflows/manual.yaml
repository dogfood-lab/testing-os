name: Manual

on: workflow_dispatch

permissions: read-all

jobs:
  say:
    runs-on: ubuntu-latest
    steps:
      - run: echo manual
      - run: node --test --test-concurrency=1 lib/schema.js
      - run: echo "$(node -p "require('./package.json').name")"
