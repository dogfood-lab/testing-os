summary: a small repository whose workflows are its doors
boundaries:
  - name: indexes
    globs:
      - indexes/**
    role: config
  - name: lib
    globs:
      - lib/**
    role: code
  - name: policies
    globs:
      - policies/**
    role: config
  - name: records
    globs:
      - records/**
    role: config
  - name: reports
    globs:
      - reports/**
    role: config
  - name: root
    globs:
      - "*"
    role: config
  - name: site
    globs:
      - site/**
    role: docs
  - name: tools
    globs:
      - tools/**
    role: code
  - name: workflows
    globs:
      - .github/**
    role: config
