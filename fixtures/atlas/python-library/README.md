# python-library

An Atlas fixture for a Python library with no command, the shape
record-index has: `pyproject.toml` names the project `record-index` and
declares no scripts, the `record_index` package is what people import, and
a release uploads it to PyPI. The first line says people import it.
