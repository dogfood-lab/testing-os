# language-share

An Atlas fixture for the first line's language list, the shape commandui
has: a TypeScript web half that holds a bare majority of the code, a Rust
crate nearly as large, and one JavaScript configuration file. The line names
the language that holds most of the code, and every other language after it.
