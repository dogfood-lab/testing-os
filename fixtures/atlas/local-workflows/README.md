# local-workflows

An Atlas fixture for a release that calls this repository's own CI as a
reusable workflow and packs through a composite action it ships, the shapes
escape-the-valley, tool-compass and armature have. The release runs what
CI runs and what the action's steps run.
