# negated-gates

An Atlas fixture. pages deploys only when the event is not a pull request,
which leaves a push to main (or a run by hand); nightly publishes only when
the event is not a pull request, which leaves a push to main or the schedule.
