name: nightly
on:
  push:
    branches: [main]
  schedule:
    - cron: '0 3 * * *'
  pull_request:
jobs:
  publish:
    if: ${{ !(github.event_name == 'pull_request') }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: npm publish
