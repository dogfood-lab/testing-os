# command-files

A release that bundles a script with a packager the map does not know and
then runs a command the project installs by its path in a virtual
environment, and three site deploys that build the Astro site from site/.
