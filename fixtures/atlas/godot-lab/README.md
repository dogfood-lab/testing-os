# godot-lab

An Atlas fixture for a Godot project that is a render lab, not a game, the
shape sprite-foundry's `game/godot/render-lab` has: `config/name` is
"Foundry Render Lab" with a main scene. The page calls it the Godot
project, never "the game".
