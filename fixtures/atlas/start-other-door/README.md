# start-other-door

An Atlas fixture for where to start when the busiest door runs no code, the
shape the organization's .github repository has. A pull request's guard
hands the community files to a checker and runs nothing; a second pull
request door runs a script that imports nothing but reads the catalog
another script writes. The path follows the second door, from the check to
the catalog and the script that writes it.
