# caller-paths

A command people run that writes into the directory they run it in and into
their home directory, beside a tracked data/ of the same name, and a script a
workflow runs from the repository root that writes into data/ here.
