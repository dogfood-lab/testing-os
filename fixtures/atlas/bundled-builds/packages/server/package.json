{
  "name": "@bundled/server",
  "version": "1.0.0",
  "type": "module",
  "bin": { "bundled-server": "dist/server.js" },
  "scripts": { "build": "esbuild src/server.ts --bundle --platform=node --format=esm --outfile=dist/server.js" }
}
