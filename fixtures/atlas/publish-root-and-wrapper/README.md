# publish-root-and-wrapper

An Atlas fixture for a repository that publishes its own package on a tag
and, by hand, a wrapper package another workflow picks at run time, the
shape npm-launcher has. The page names the repository's own package beside
the wrapper, so neither publish hides the other.
