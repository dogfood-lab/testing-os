# publish-root-and-wrapper

An Atlas fixture for a repository that publishes its own package on a tag
and, by hand, a wrapper package from the directory under examples/ a
dispatch input names, the shape npm-launcher has. examples/tool is the one
directory the input can name, so the page names the repository's own
package beside that wrapper, and neither publish hides the other.
