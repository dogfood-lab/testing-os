# unresolved-once

An Atlas fixture for one unresolvable import a file makes twice, the shape
portlight's `encounter.py` and comfy-preflight's packaging test have: two
lazy `from app.content.weapons import ...` lines naming a module that is
not there. The page says the clause once, with how many sites it is.
