summary: ""
boundaries:
  - name: .github
    globs:
      - .github/**
    role: config
  - name: root
    globs:
      - "*"
    role: config
  - name: data
    globs:
      - data/**
    role: data
  - name: one
    globs:
      - packages/one/**
    role: code
  - name: two
    globs:
      - packages/two/**
    role: code
