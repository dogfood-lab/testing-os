summary: a small host fixture
boundaries:
  - name: alpha
    globs:
      - pkg/alpha/**
    role: code
  - name: beta
    globs:
      - pkg/beta/**
    role: code
  - name: tests
    globs:
      - tests/**
    role: test
