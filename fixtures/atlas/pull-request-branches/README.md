# pull-request-branches

An Atlas fixture for a pull request filtered by the branch it targets. CI
runs on a pull request to main, touching two paths, and on a push to main;
the pull request's branch is said as the push's is.
