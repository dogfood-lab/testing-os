name: CI
on:
  pull_request:
    branches: [main]
    paths: ['scripts/**', 'package.json']
  push:
    branches: [main]
jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - run: node scripts/check.mjs
