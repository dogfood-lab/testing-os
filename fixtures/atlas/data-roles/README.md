# data-roles

An Atlas fixture read by atlas init. logos/ holds images; records/ JSON
Lines; schemas/ JSON schemas; world/ YAML and JSON data; config/ the
settings files tools read; scripts/ shell scripts beside a README; src/ code.
