# grammars-rust-gdscript

An Atlas fixture for the two languages read with a vendored grammar besides
JavaScript, TypeScript and Python: a Rust crate of three files, one of which
does not parse, and a Godot project of three GDScript files, one of which
does not parse, with a scene and the project file, which are read as text.
