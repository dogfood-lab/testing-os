# test-place-readers

An Atlas fixture for a place code writes and code and tests read, the
shape comfy-preflight's `tests/fixtures/graphs/` has: `tools/make.py`
writes `data/graphs.json`, two scripts read it, and the test suite reads
it too. The sentence on what a hand edit breaks names the tests among its
readers.
