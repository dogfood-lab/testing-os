# package-role

An Atlas fixture for a package directory that carries the workflows of
the repository it came from, the shape accessibility-suite's
`src/a11y-assist` has: a `pyproject.toml`, a Python package and a
`.github/workflows/` of its own. `atlas init` proposes it as code, never
config.
