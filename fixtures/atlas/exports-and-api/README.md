# exports-and-api

An Atlas fixture. The package exports five subpaths that name files (one
through an import condition beside its types) and its own package.json; a
release workflow publishes it. The sync command opens issues and pull requests
in other repositories through the GitHub API, reads one without changing it,
and opens an issue on the repository it runs in.
