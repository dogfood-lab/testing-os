# next-app

An Atlas fixture for a Next.js app with the App Router under src/app/, the
shape motif's studio has. CI runs the app's build script, next build. The
app part's entry points are its page, layout and route files, and CI runs
its next.config.js and src/app/.
