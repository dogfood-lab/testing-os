# review-pushes

An Atlas fixture. sync logos and freshness commit onto a branch of their own
and push that branch for review; render force-pushes its commit to a render
branch kept for its own sake; release commits CHANGELOG.md and pushes to
main. sync logos reaches two parts and release one.
