# init-role-ties

An Atlas fixture for the roles init gives directories on the edge, the
shapes websketch-ir, portlight and audiobooker have. docs/ holds one page
and one config file, a tie that is docs; artifacts/ holds a JSON and a
Markdown report per run, which is data; npm/ is a wrapper package with a
command and its READMEs, which is code.
