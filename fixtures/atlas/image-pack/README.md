# image-pack

A repository that is mostly images, the way sprite-foundry-packs is: a pack of
sprites published as an npm package, a manifest beside them, and a little code
in three languages. The first line says the images dominate and names every
language, and the pack is data though it holds a package.json.
