# own-steps

An Atlas fixture for the order of work of a Python entry that calls string
methods and a test helper beside the project's own functions, the shape
armature and tool-compass have. The steps the page names are the
project's own functions, written as the code spells them.
