# unread-code

An Atlas fixture for code parts that hold only code this map does not
read, the shapes site-theme and registry-stats have: a stylesheet the
package ships and a C# desktop app beside a Python tool its tests import. The page names the
parts it cannot read in What breaks what and What no test touches, and
never says every code part is imported by a test.
