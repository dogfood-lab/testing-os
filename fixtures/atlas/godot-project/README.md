# godot-project

An Atlas fixture for what a Godot project declares, the shape ai-rpg-stage
has. project.godot names the main scene the game starts and two autoloads
the engine loads before it, and holds an input map written across lines;
export_presets.cfg names the presets a release is exported with. The
engine's .godot/ cache is ignored.
