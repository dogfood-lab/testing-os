# private-command

An Atlas fixture for a command a private package declares, the shape
ai-playtest has: package.json is private and has a bin, and nothing
publishes or ships it. The page says nothing ships it.
