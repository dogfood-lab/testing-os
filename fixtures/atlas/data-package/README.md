# data-package

An Atlas fixture for a package whose every export is JSON, the shape
mcp-tool-registry has, beside a CI that runs code and a release that
publishes it. The package ships data and loads nothing, so it is the data
package people import, and CI, which runs code, is the busiest door though
the package's data reaches more.
