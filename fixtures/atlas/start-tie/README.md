# start-tie

An Atlas fixture for a tie among four doors: a pull request's CI and a
release's Publish that only type-check src/, the package's entry and a
command, each reaching the one part. The page names every tied door, breaks
the tie toward the pull request's door although it only checks, and follows
the command from its entry, since that door runs no code.
