{
  "name": "@acme/server",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "bin": { "acme-server": "dist/server.js" },
  "scripts": { "build": "esbuild src/server.ts --bundle --platform=node --outfile=dist/server.js" }
}
