# npm-wrapper-publish

An Atlas fixture for a release that publishes one npm wrapper from a
directory named npm/, the shape armature and audiobooker have. The page
names the package alone, never "@w/tool (npm) to npm".
