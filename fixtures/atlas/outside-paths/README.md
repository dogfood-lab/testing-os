# outside-paths

A research script that loads a build from a path on one machine's disk, from
the home directory and from a sibling checkout: none of them is in this
repository, and the map must say the same about each on every host.
