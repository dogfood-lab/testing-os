# docs-gate

An Atlas fixture for a companion workflow that satisfies required check
names on pull requests CI skips, the shape site-theme's `ci-docs.yml`
has: it runs on a pull request except when only the paths CI covers
change, and its jobs only echo. The trigger is said with the paths it
ignores, and the door as running only echo, never as running something
the map cannot see.
