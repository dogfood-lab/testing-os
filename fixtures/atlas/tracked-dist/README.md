# tracked-dist

An Atlas fixture for a dist/ the repository commits, the shape
mcp-tool-registry has: a script writes dist/registry.json, which is
tracked and shipped in the package's files. A tracked directory is a place
whatever its name, so dist/ is generated, never "not tracked".
