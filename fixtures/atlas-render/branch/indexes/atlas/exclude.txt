# Public repositories named here are not rendered.
# One owner/repo per line. A line starting with # is a comment.
# Read from the default branch. The weekly job does not write this file,
# because a human decision must not ride the branch the job overwrites.
