{
  "description": "Allowlist for the F-8125c01b orphaned-test discovery sweep in scripts/stageC-check-test-floor.test.mjs. Every committed *.test.{js,mjs,cjs,ts} file must be matched by its owning package's test script, by the root test:scripts globs, or by an entry here WITH a reason (the scripts/regression-pin-allowlist.json discipline). Entries whose path no longer exists fail the sweep — no rot.",
  "entries": [
    {
      "path": "fixtures/atlas/basic/packages/beta/beta.test.js",
      "reason": "Atlas fixture, not a test. It is a file whose test-shaped name is the point: it sits inside the fixture repository that packages/atlas/core tests map, and a later slice derives a boundary's test role from exactly this naming convention. It contains no test and is never meant to run; running it would prove nothing about Atlas."
    },
    {
      "path": "fixtures/atlas/doors/lib/verify.test.js",
      "reason": "Atlas fixture, not a test. Its test-shaped name is the point: it sits inside the doors fixture repository that packages/atlas/adapter/page.test.js maps, and it imports lib/verify.js so that the map counts one test file reaching lib and none reaching tools. It contains no test and is never meant to run."
    },
    {
      "path": "fixtures/atlas/doors-ts/packages/core/src/core.test.ts",
      "reason": "Atlas fixture, not a test. It sits inside the doors-ts fixture repository that packages/atlas/core/doors-conventions.test.js maps, where the fixture's vitest include and its tsconfig.tests.json pattern must select it by exactly this name. It holds no test and is never meant to run."
    },
    {
      "path": "fixtures/atlas/doors-ts/tools/check.test.js",
      "reason": "Atlas fixture, not a test. It sits inside the doors-ts fixture repository, where a door runs node --test from tools/ with no path and the default patterns must select it by exactly this name. It holds no test and is never meant to run."
    },
    {
      "path": "fixtures/atlas/doors-ts/test/unit.spec.js",
      "reason": "Atlas fixture, not a test. It sits inside the doors-ts fixture repository, where a door runs mocha with no path and the fixture's .mocharc.yml spec must select it by exactly this name. It holds no test and is never meant to run."
    },
    {
      "path": "fixtures/atlas/ts-probe/packages/cli/src/cli.spec.ts",
      "reason": "Atlas fixture, not a test. It sits inside the ts-probe fixture repository that packages/atlas/adapter/ts-probe.test.js maps, and it is the only file in cli that imports starter-a, so the map records that edge as imported only from tests. It holds no test and is never meant to run."
    },
    {
      "path": "fixtures/atlas/ts-probe/packages/core/src/engine.spec.ts",
      "reason": "Atlas fixture, not a test. It sits inside the ts-probe fixture repository, where atlas explain must name it as engine.ts's own test by exactly this name. It holds no test and is never meant to run."
    },
    {
      "path": "fixtures/atlas/ts-probe/packages/ledger/src/ledger.spec.ts",
      "reason": "Atlas fixture, not a test. It sits inside the ts-probe fixture repository, and it is the only file in ledger that imports core, so the map records that edge as imported only from tests. It holds no test and is never meant to run."
    },
    {
      "path": "fixtures/atlas/ts-probe/scripts/gates.spec.ts",
      "reason": "Atlas fixture, not a test. It sits inside the ts-probe fixture repository, where it runs scripts/check.mjs through a spawnSync command written out in full, so the map counts the scripts part as reached by a test. It holds no test and is never meant to run."
    },
    {
      "path": "fixtures/atlas/commands/tests/tool.test.mjs",
      "reason": "Atlas fixture, not a test. It sits inside the commands fixture repository that packages/atlas/core/command-doors.test.js and packages/atlas/adapter/page-commands.test.js map, where CI runs node --test tests/ and must select it by exactly this name so the workflow door reaches lib beside the commands the manifests install. It is never meant to run here."
    },
    {
      "path": "fixtures/atlas/checked-runs/test/check.test.js",
      "reason": "Atlas fixture, not a test. It sits inside the checked-runs fixture repository that packages/atlas/adapter/page-checked-runs.test.js maps, where CI runs node --test test/ over it while eslint only checks the tree around it, so the map must record test/ as executed. It is never meant to run here."
    },
    {
      "path": "fixtures/atlas/limits-split/tests/cli.test.js",
      "reason": "Atlas fixture, not a test. It sits inside the limits-split fixture repository that packages/atlas/adapter/page-limits.test.js maps, where the two commands it builds at run time must be counted as commands built in tests. It is never meant to run here."
    },
    {
      "path": "fixtures/atlas/init-roles/tools/index.test.js",
      "reason": "Atlas fixture, not a test. It sits inside the init-roles fixture repository that packages/atlas/adapter/init-roles.test.js runs atlas init on: its name starts with index., the first name init falls back to for a part's entry point, and init must propose tools/cli.js beside it instead. It holds no test and is never meant to run."
    },
    {
      "path": "fixtures/atlas/authored-caveat/packages/export/src/__tests__/table.test.ts",
      "reason": "Atlas fixture, not a test. It sits inside the authored-caveat fixture repository that packages/atlas/adapter/page-authored-caveat.test.js maps, where it must be read as a test, by exactly this name, that rewrites a committed table under docs/ through import.meta.dirname. It holds no test and is never meant to run."
    },
    {
      "path": "fixtures/atlas/start-chain/test/app.test.js",
      "reason": "Atlas fixture, not a test. It sits inside the start-chain fixture repository that packages/atlas/adapter/page-start-chain.test.js maps, where the pull-request door runs test/ and \"Where to start\" must name this file, by exactly this name, as the one in the directory that imports into the code. It holds no test and is never meant to run."
    },
    {
      "path": "fixtures/atlas/authored-caveat/packages/export/src/__tests__/scratch.test.ts",
      "reason": "Atlas fixture, not a test. It sits inside the authored-caveat fixture repository that packages/atlas/adapter/page-authored-caveat.test.js maps, where it must be read as a test, by exactly this name, that writes a scratch file beside itself under a name built at run time, which is no write to the repository. It holds no test and is never meant to run."
    },
    {
      "path": "fixtures/swarm-atlas/mapped-repo/tests/core.test.js",
      "reason": "Swarm fixture, not a test. It sits inside the mapped fixture repository that packages/dogfood-swarm/atlas-domain-map.test.js, atlas-brief.test.js and atlas-delta.test.js copy into a temporary git repository; its tests part has role test and this file imports core and api, so the map records both edges as imported only from tests and the domain draft must fold the tests part into one of them. It is never meant to run."
    },
    {
      "path": "fixtures/swarm-atlas/unmapped-repo/tests/server.test.js",
      "reason": "Swarm fixture, not a test. It sits inside the unmapped fixture repository that packages/dogfood-swarm/atlas-domain-map.test.js drafts from the hand template, whose tests bucket must claim it by exactly this name so the template map is the one a real repository without a boundary file gets. It is never meant to run."
    },
    {
      "path": "fixtures/atlas/package-managers/packages/a/test/a.test.js",
      "reason": "Atlas fixture, not a test. It sits inside the package-managers fixture repository that packages/atlas/core/package-managers.test.js maps, where `pnpm -r test` must reach packages/a/test/ through the member's own test script. It holds one empty test and is never meant to run."
    },
    {
      "path": "fixtures/atlas/package-managers/packages/b/test/b.test.js",
      "reason": "Atlas fixture, not a test. It sits inside the package-managers fixture repository that packages/atlas/core/package-managers.test.js maps, where `pnpm -r test` must reach packages/b/test/ through the member's own test script. It holds one empty test and is never meant to run."
    },
    {
      "path": "fixtures/atlas/package-managers/test/root.test.js",
      "reason": "Atlas fixture, not a test. It sits inside the package-managers fixture repository that packages/atlas/core/package-managers.test.js maps, where `pnpm verify` must reach test/ through the root test script it calls. It holds one empty test and is never meant to run."
    },
    {
      "path": "fixtures/atlas/test-readers/test/golden.test.js",
      "reason": "Atlas fixture, not a test. It sits inside the test-readers fixture repository that packages/atlas/adapter/page-test-readers.test.js maps, where it must be read as a test, by exactly this name, that reads fixtures/golden.json by its path, so the map records it as a reader from tests. It is never meant to run."
    },
    {
      "path": "fixtures/atlas/spawn-helpers/test/unit.test.js",
      "reason": "Atlas fixture, not a test. It sits inside the spawn-helpers fixture repository that packages/atlas/core/spawn-helpers.test.js maps, where the release gate's `node --test test/` stage, handed to a helper, must reach test/ as a run. It holds one empty test and is never meant to run."
    },
    {
      "path": "fixtures/atlas/start-edges/e2e/big.test.js",
      "reason": "Atlas fixture, not a test. It sits inside the start-edges fixture repository that packages/atlas/adapter/page-start-edges.test.js maps, where CI's e2e job, held to a path filter's needs.changes output, runs it and reaches further than the test job, so the map must not start Where to start there. It is never meant to run."
    },
    {
      "path": "fixtures/atlas/start-edges/test/run.test.js",
      "reason": "Atlas fixture, not a test. It sits inside the start-edges fixture repository that packages/atlas/adapter/page-start-edges.test.js maps, where it must be the first file of Where to start, past the empty test/a-setup.js, and pass over itself to the src/run.js it imports. It is never meant to run."
    },
    {
      "path": "fixtures/atlas/exports-and-api/test/apply.test.js",
      "reason": "Atlas fixture, not a test. It sits inside the exports-and-api fixture repository that packages/atlas/adapter/page-exports-and-api.test.js maps, where CI's suite imports the file that changes other repositories through the GitHub API and runs it against a stand-in, so the map must not say CI changes other repositories. It is never meant to run."
    }
  ]
}
