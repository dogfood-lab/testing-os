{
  "description": "Allowlist for the F-8125c01b orphaned-test discovery sweep in scripts/stageC-check-test-floor.test.mjs. Every committed *.test.{js,mjs,cjs,ts} file must be matched by its owning package's test script, by the root test:scripts globs, or by an entry here WITH a reason (the scripts/regression-pin-allowlist.json discipline). Entries whose path no longer exists fail the sweep — no rot.",
  "entries": [
    {
      "path": "fixtures/atlas/basic/packages/beta/beta.test.js",
      "reason": "Atlas fixture, not a test. It is a file whose test-shaped name is the point: it sits inside the fixture repository that packages/atlas/core tests map, and a later slice derives a boundary's test role from exactly this naming convention. It contains no test and is never meant to run; running it would prove nothing about Atlas."
    },
    {
      "path": "fixtures/atlas/doors/lib/verify.test.js",
      "reason": "Atlas fixture, not a test. Its test-shaped name is the point: it sits inside the doors fixture repository that packages/atlas/adapter/page.test.js maps, and it imports lib/verify.js so that the map counts one test file reaching lib and none reaching tools. It contains no test and is never meant to run."
    },
    {
      "path": "fixtures/atlas/doors-ts/packages/core/src/core.test.ts",
      "reason": "Atlas fixture, not a test. It sits inside the doors-ts fixture repository that packages/atlas/core/doors-conventions.test.js maps, where the fixture's vitest include and its tsconfig.tests.json pattern must select it by exactly this name. It holds no test and is never meant to run."
    },
    {
      "path": "fixtures/atlas/doors-ts/tools/check.test.js",
      "reason": "Atlas fixture, not a test. It sits inside the doors-ts fixture repository, where a door runs node --test from tools/ with no path and the default patterns must select it by exactly this name. It holds no test and is never meant to run."
    },
    {
      "path": "fixtures/atlas/doors-ts/test/unit.spec.js",
      "reason": "Atlas fixture, not a test. It sits inside the doors-ts fixture repository, where a door runs mocha with no path and the fixture's .mocharc.yml spec must select it by exactly this name. It holds no test and is never meant to run."
    },
    {
      "path": "fixtures/atlas/ts-probe/packages/cli/src/cli.spec.ts",
      "reason": "Atlas fixture, not a test. It sits inside the ts-probe fixture repository that packages/atlas/adapter/ts-probe.test.js maps, and it is the only file in cli that imports starter-a, so the map records that edge as imported only from tests. It holds no test and is never meant to run."
    },
    {
      "path": "fixtures/atlas/ts-probe/packages/core/src/engine.spec.ts",
      "reason": "Atlas fixture, not a test. It sits inside the ts-probe fixture repository, where atlas explain must name it as engine.ts's own test by exactly this name. It holds no test and is never meant to run."
    },
    {
      "path": "fixtures/atlas/ts-probe/packages/ledger/src/ledger.spec.ts",
      "reason": "Atlas fixture, not a test. It sits inside the ts-probe fixture repository, and it is the only file in ledger that imports core, so the map records that edge as imported only from tests. It holds no test and is never meant to run."
    },
    {
      "path": "fixtures/atlas/ts-probe/scripts/gates.spec.ts",
      "reason": "Atlas fixture, not a test. It sits inside the ts-probe fixture repository, where it runs scripts/check.mjs through a spawnSync command written out in full, so the map counts the scripts part as reached by a test. It holds no test and is never meant to run."
    },
    {
      "path": "fixtures/atlas/commands/tests/tool.test.mjs",
      "reason": "Atlas fixture, not a test. It sits inside the commands fixture repository that packages/atlas/core/command-doors.test.js and packages/atlas/adapter/page-commands.test.js map, where CI runs node --test tests/ and must select it by exactly this name so the workflow door reaches lib beside the commands the manifests install. It is never meant to run here."
    },
    {
      "path": "fixtures/atlas/checked-runs/test/check.test.js",
      "reason": "Atlas fixture, not a test. It sits inside the checked-runs fixture repository that packages/atlas/adapter/page-checked-runs.test.js maps, where CI runs node --test test/ over it while eslint only checks the tree around it, so the map must record test/ as executed. It is never meant to run here."
    },
    {
      "path": "fixtures/atlas/limits-split/tests/cli.test.js",
      "reason": "Atlas fixture, not a test. It sits inside the limits-split fixture repository that packages/atlas/adapter/page-limits.test.js maps, where the two commands it builds at run time must be counted as commands built in tests. It is never meant to run here."
    },
    {
      "path": "fixtures/atlas/init-roles/tools/index.test.js",
      "reason": "Atlas fixture, not a test. It sits inside the init-roles fixture repository that packages/atlas/adapter/init-roles.test.js runs atlas init on: its name starts with index., the first name init falls back to for a part's entry point, and init must propose tools/cli.js beside it instead. It holds no test and is never meant to run."
    },
    {
      "path": "fixtures/atlas/authored-caveat/packages/export/src/__tests__/table.test.ts",
      "reason": "Atlas fixture, not a test. It sits inside the authored-caveat fixture repository that packages/atlas/adapter/page-authored-caveat.test.js maps, where it must be read as a test, by exactly this name, that rewrites a committed table under docs/ through import.meta.dirname. It holds no test and is never meant to run."
    },
    {
      "path": "fixtures/atlas/start-chain/test/app.test.js",
      "reason": "Atlas fixture, not a test. It sits inside the start-chain fixture repository that packages/atlas/adapter/page-start-chain.test.js maps, where the pull-request door runs test/ and \"Where to start\" must pass over this file, by exactly this name, to the code it imports, as the one in the directory that imports into the code. It holds no test and is never meant to run."
    },
    {
      "path": "fixtures/atlas/authored-caveat/packages/export/src/__tests__/scratch.test.ts",
      "reason": "Atlas fixture, not a test. It sits inside the authored-caveat fixture repository that packages/atlas/adapter/page-authored-caveat.test.js maps, where it must be read as a test, by exactly this name, that writes a scratch file beside itself under a name built at run time, which is no write to the repository. It holds no test and is never meant to run."
    },
    {
      "path": "fixtures/swarm-atlas/mapped-repo/tests/core.test.js",
      "reason": "Swarm fixture, not a test. It sits inside the mapped fixture repository that packages/dogfood-swarm/atlas-domain-map.test.js, atlas-brief.test.js and atlas-delta.test.js copy into a temporary git repository; its tests part has role test and this file imports core and api, so the map records both edges as imported only from tests and the domain draft must fold the tests part into one of them. It is never meant to run."
    },
    {
      "path": "fixtures/swarm-atlas/unmapped-repo/tests/server.test.js",
      "reason": "Swarm fixture, not a test. It sits inside the unmapped fixture repository that packages/dogfood-swarm/atlas-domain-map.test.js drafts from the hand template, whose tests bucket must claim it by exactly this name so the template map is the one a real repository without a boundary file gets. It is never meant to run."
    },
    {
      "path": "fixtures/atlas/package-managers/packages/a/test/a.test.js",
      "reason": "Atlas fixture, not a test. It sits inside the package-managers fixture repository that packages/atlas/core/package-managers.test.js maps, where `pnpm -r test` must reach packages/a/test/ through the member's own test script. It holds one empty test and is never meant to run."
    },
    {
      "path": "fixtures/atlas/package-managers/packages/b/test/b.test.js",
      "reason": "Atlas fixture, not a test. It sits inside the package-managers fixture repository that packages/atlas/core/package-managers.test.js maps, where `pnpm -r test` must reach packages/b/test/ through the member's own test script. It holds one empty test and is never meant to run."
    },
    {
      "path": "fixtures/atlas/package-managers/test/root.test.js",
      "reason": "Atlas fixture, not a test. It sits inside the package-managers fixture repository that packages/atlas/core/package-managers.test.js maps, where `pnpm verify` must reach test/ through the root test script it calls. It holds one empty test and is never meant to run."
    },
    {
      "path": "fixtures/atlas/test-readers/test/golden.test.js",
      "reason": "Atlas fixture, not a test. It sits inside the test-readers fixture repository that packages/atlas/adapter/page-test-readers.test.js maps, where it must be read as a test, by exactly this name, that reads fixtures/golden.json by its path, so the map records it as a reader from tests. It is never meant to run."
    },
    {
      "path": "fixtures/atlas/spawn-helpers/test/unit.test.js",
      "reason": "Atlas fixture, not a test. It sits inside the spawn-helpers fixture repository that packages/atlas/core/spawn-helpers.test.js maps, where the release gate's `node --test test/` stage, handed to a helper, must reach test/ as a run. It holds one empty test and is never meant to run."
    },
    {
      "path": "fixtures/atlas/start-edges/e2e/big.test.js",
      "reason": "Atlas fixture, not a test. It sits inside the start-edges fixture repository that packages/atlas/adapter/page-start-edges.test.js maps, where CI's e2e job, held to a path filter's needs.changes output, runs it and reaches further than the test job, so the map must not start Where to start there. It is never meant to run."
    },
    {
      "path": "fixtures/atlas/start-edges/test/run.test.js",
      "reason": "Atlas fixture, not a test. It sits inside the start-edges fixture repository that packages/atlas/adapter/page-start-edges.test.js maps, where Where to start must pass over it, past the empty test/a-setup.js, to the src/run.js it imports. It is never meant to run."
    },
    {
      "path": "fixtures/atlas/start-door/release/packages/core/test/core.test.js",
      "reason": "Atlas fixture, not a test. It sits inside the start-door fixture repository that packages/atlas/adapter/page-start-door.test.js maps, where the pull request's door runs it, and Where to start must pass over it to the entry of the core package it imports. It holds no test and is never meant to run."
    },
    {
      "path": "fixtures/atlas/test-writes/packages/app/src/materialize.test.ts",
      "reason": "Atlas fixture, not a test. It sits inside the test-writes fixture repository that packages/atlas/core/test-writes.test.js maps, where it must rewrite the committed examples/ through a helper it hands paths built from its own location, and be listed as generated by a test. It is never meant to run."
    },
    {
      "path": "fixtures/atlas/exports-and-api/test/apply.test.js",
      "reason": "Atlas fixture, not a test. It sits inside the exports-and-api fixture repository that packages/atlas/adapter/page-exports-and-api.test.js maps, where CI's suite imports the file that changes other repositories through the GitHub API and runs it against a stand-in, so the map must not say CI changes other repositories. It is never meant to run."
    },
    {
      "path": "fixtures/atlas/start-tests/command/tests/run.test.js",
      "reason": "Atlas fixture, not a test. It sits inside the start-tests fixture repository that packages/atlas/adapter/page-start-tests.test.js maps, where the pull request's door runs it and nothing else, so Where to start must leave it for the command the manifest installs. It holds no test and is never meant to run."
    },
    {
      "path": "fixtures/atlas/start-tests/package/tests/parse.test.js",
      "reason": "Atlas fixture, not a test. It sits inside the start-tests fixture repository that packages/atlas/adapter/page-start-tests.test.js maps, where the pull request's door runs only this test, so Where to start follows the package's entry instead. It holds no test and is never meant to run."
    },
    {
      "path": "fixtures/atlas/start-tests/constant/tests/work.test.js",
      "reason": "Atlas fixture, not a test. It sits inside the start-tests fixture repository that packages/atlas/adapter/page-start-tests.test.js maps, where it imports a file holding one constant before the file doing the work, and Where to start must pass over the constant to the work. It holds no test and is never meant to run."
    },
    {
      "path": "fixtures/atlas/spawned-scripts/tests/smoke.test.js",
      "reason": "Atlas fixture, not a test. It sits inside the spawned-scripts fixture repository that packages/atlas/adapter/page-spawned-scripts.test.js maps, where the tests either run scripts/ as a child process or import src/, so the map counts scripts as touched only through a spawn. It holds no test and is never meant to run."
    },
    {
      "path": "fixtures/atlas/spawned-scripts/tests/run-gate.test.js",
      "reason": "Atlas fixture, not a test. It sits inside the spawned-scripts fixture repository that packages/atlas/adapter/page-spawned-scripts.test.js maps, where the tests either run scripts/ as a child process or import src/, so the map counts scripts as touched only through a spawn. It holds no test and is never meant to run."
    },
    {
      "path": "fixtures/atlas/spawned-scripts/tests/add.test.js",
      "reason": "Atlas fixture, not a test. It sits inside the spawned-scripts fixture repository that packages/atlas/adapter/page-spawned-scripts.test.js maps, where the tests either run scripts/ as a child process or import src/, so the map counts scripts as touched only through a spawn. It holds no test and is never meant to run."
    },
    {
      "path": "fixtures/atlas/shell-globs/src/a.test.js",
      "reason": "Atlas fixture, not a test. It sits inside the shell-globs fixture repository that packages/atlas/adapter/page-shell-globs.test.js maps, where a node --test glob selects it by this name, and where it sits decides whether sh's expansion of ** reaches it on Linux. It holds no test and is never meant to run."
    },
    {
      "path": "fixtures/atlas/shell-globs/src/d.test.js",
      "reason": "Atlas fixture, not a test. It sits inside the shell-globs fixture repository that packages/atlas/adapter/page-shell-globs.test.js maps, where a node --test glob selects it by this name, and where it sits decides whether sh's expansion of ** reaches it on Linux. It holds no test and is never meant to run."
    },
    {
      "path": "fixtures/atlas/shell-globs/src/deep/b.test.js",
      "reason": "Atlas fixture, not a test. It sits inside the shell-globs fixture repository that packages/atlas/adapter/page-shell-globs.test.js maps, where a node --test glob selects it by this name, and where it sits decides whether sh's expansion of ** reaches it on Linux. It holds no test and is never meant to run."
    },
    {
      "path": "fixtures/atlas/shell-globs/src/deep/deeper/c.test.js",
      "reason": "Atlas fixture, not a test. It sits inside the shell-globs fixture repository that packages/atlas/adapter/page-shell-globs.test.js maps, where a node --test glob selects it by this name, and where it sits decides whether sh's expansion of ** reaches it on Linux. It holds no test and is never meant to run."
    },
    {
      "path": "fixtures/atlas/shell-globs/tools/lib/e.test.js",
      "reason": "Atlas fixture, not a test. It sits inside the shell-globs fixture repository that packages/atlas/adapter/page-shell-globs.test.js maps, where a node --test glob selects it by this name, and where it sits decides whether sh's expansion of ** reaches it on Linux. It holds no test and is never meant to run."
    },
    {
      "path": "fixtures/atlas/shell-globs/tools/lib/inner/f.test.js",
      "reason": "Atlas fixture, not a test. It sits inside the shell-globs fixture repository that packages/atlas/adapter/page-shell-globs.test.js maps, where a node --test glob selects it by this name, and where it sits decides whether sh's expansion of ** reaches it on Linux. It holds no test and is never meant to run."
    },
    {
      "path": "fixtures/atlas/built-tests/src/tests/total.test.ts",
      "reason": "Atlas fixture, not a test. It sits inside the built-tests fixture repository that packages/atlas/core/built-tests.test.js maps, where node --test is handed a glob over dist/ and must run this source by the name its build output takes. It holds no test and is never meant to run."
    },
    {
      "path": "fixtures/atlas/built-tests/src/tests/unit/sum.test.ts",
      "reason": "Atlas fixture, not a test. It sits inside the built-tests fixture repository that packages/atlas/core/built-tests.test.js maps, where node --test is handed a glob over dist/ and must run this source by the name its build output takes. It holds no test and is never meant to run."
    },
    {
      "path": "fixtures/atlas/grammar-shapes/src/mint.test.ts",
      "reason": "Atlas fixture, not a test. It sits inside the grammar-shapes fixture repository that packages/atlas/core/grammar-shapes.test.js maps, where it holds typeof import(...) as a type argument, the shape vitest's importOriginal mocks take, which the map must read by rewriting it. It holds no test and is never meant to run."
    },
    {
      "path": "fixtures/atlas/run-counts/tests/a.test.js",
      "reason": "Atlas fixture, not a test. It sits inside the run-counts fixture repository that packages/atlas/adapter/page-run-counts.test.js maps, where node --test runs it by name or through a glob, and the page must count it among the files its door runs. It holds no test and is never meant to run."
    },
    {
      "path": "fixtures/atlas/run-counts/tests/b.test.js",
      "reason": "Atlas fixture, not a test. It sits inside the run-counts fixture repository that packages/atlas/adapter/page-run-counts.test.js maps, where node --test runs it by name or through a glob, and the page must count it among the files its door runs. It holds no test and is never meant to run."
    },
    {
      "path": "fixtures/atlas/run-counts/tests/c.test.js",
      "reason": "Atlas fixture, not a test. It sits inside the run-counts fixture repository that packages/atlas/adapter/page-run-counts.test.js maps, where node --test runs it by name or through a glob, and the page must count it among the files its door runs. It holds no test and is never meant to run."
    },
    {
      "path": "fixtures/atlas/run-counts/tests/d.test.js",
      "reason": "Atlas fixture, not a test. It sits inside the run-counts fixture repository that packages/atlas/adapter/page-run-counts.test.js maps, where node --test runs it by name or through a glob, and the page must count it among the files its door runs. It holds no test and is never meant to run."
    },
    {
      "path": "fixtures/atlas/run-counts/tests/deep/e.test.js",
      "reason": "Atlas fixture, not a test. It sits inside the run-counts fixture repository that packages/atlas/adapter/page-run-counts.test.js maps, where node --test runs it by name or through a glob, and the page must count it among the files its door runs. It holds no test and is never meant to run."
    },
    {
      "path": "fixtures/atlas/run-counts/tests/deep/f.test.js",
      "reason": "Atlas fixture, not a test. It sits inside the run-counts fixture repository that packages/atlas/adapter/page-run-counts.test.js maps, where node --test runs it by name or through a glob, and the page must count it among the files its door runs. It holds no test and is never meant to run."
    },
    {
      "path": "fixtures/atlas/run-counts/tests/deep/g.test.js",
      "reason": "Atlas fixture, not a test. It sits inside the run-counts fixture repository that packages/atlas/adapter/page-run-counts.test.js maps, where node --test runs it by name or through a glob, and the page must count it among the files its door runs. It holds no test and is never meant to run."
    },
    {
      "path": "fixtures/atlas/run-counts/tests/deep/h.test.js",
      "reason": "Atlas fixture, not a test. It sits inside the run-counts fixture repository that packages/atlas/adapter/page-run-counts.test.js maps, where node --test runs it by name or through a glob, and the page must count it among the files its door runs. It holds no test and is never meant to run."
    },
    {
      "path": "fixtures/atlas/run-counts/tests/deep/i.test.js",
      "reason": "Atlas fixture, not a test. It sits inside the run-counts fixture repository that packages/atlas/adapter/page-run-counts.test.js maps, where node --test runs it by name or through a glob, and the page must count it among the files its door runs. It holds no test and is never meant to run."
    },
    {
      "path": "fixtures/atlas/main-guards/test/generators.test.mjs",
      "reason": "Atlas fixture, not a test. It sits inside the main-guards fixture repository that packages/atlas/adapter/page-main-guards.test.js maps, where CI's node --test runs it and it imports the generators' pure functions, so the page must credit CI with none of the writes behind their main guards. It is never meant to run."
    },
    {
      "path": "fixtures/atlas/grammar-shapes/src/trailing.test.ts",
      "reason": "Atlas fixture, not a test. It sits inside the grammar-shapes fixture repository that packages/atlas/core/grammar-shapes.test.js maps, where the grammar must read typeof import(...) on a line of its own with a trailing comma, the vitest importOriginal mock shape, and record its specifier. It holds no test and is never meant to run."
    },
    {
      "path": "fixtures/atlas/start-order/own-part/test/cli.test.js",
      "reason": "Atlas fixture, not a test. It sits inside the start-order fixture repository that packages/atlas/adapter/page-start-order.test.js maps, where CI's node --test runs it and it imports the command's entry, so CI runs only tests and the path follows the command people run. It holds no test and is never meant to run."
    },
    {
      "path": "fixtures/atlas/start-order/single/test/cli.test.js",
      "reason": "Atlas fixture, not a test. It sits inside the start-order fixture repository that packages/atlas/adapter/page-start-order.test.js maps, where CI's node --test runs it and it imports the command's entry, so CI runs only tests and the path follows the command people run. It holds no test and is never meant to run."
    },
    {
      "path": "fixtures/atlas/start-order/imports/test/cli.test.js",
      "reason": "Atlas fixture, not a test. It sits inside the start-order fixture repository that packages/atlas/adapter/page-start-order.test.js maps, where CI's node --test runs it and it imports one command module, so CI runs only tests and the path follows the command people run. It holds no test and is never meant to run."
    },
    {
      "path": "fixtures/atlas/build-starts/tsup/test/run.test.js",
      "reason": "Atlas fixture, not a test. It sits inside the build-starts fixture repository that packages/atlas/adapter/page-build-starts.test.js maps, where CI's node --test runs it beside a build of the package, so the path must start at the package's entry rather than at a test. It holds no test and is never meant to run."
    },
    {
      "path": "fixtures/atlas/build-starts/tsc/test/run.test.js",
      "reason": "Atlas fixture, not a test. It sits inside the build-starts fixture repository that packages/atlas/adapter/page-build-starts.test.js maps, where CI's node --test runs it beside a build of the package, so the path must start at the package's entry rather than at a test. It holds no test and is never meant to run."
    },
    {
      "path": "fixtures/atlas/build-starts/vite/test/run.test.js",
      "reason": "Atlas fixture, not a test. It sits inside the build-starts fixture repository that packages/atlas/adapter/page-build-starts.test.js maps, where CI's node --test runs it beside a build of the package, so the path must start at the package's entry rather than at a test. It holds no test and is never meant to run."
    },
    {
      "path": "fixtures/atlas/start-dead-ends/gate/test/work.test.js",
      "reason": "Atlas fixture, not a test. It sits inside the start-dead-ends fixture repository that packages/atlas/adapter/page-start-dead-ends.test.js maps, where CI's node --test runs it beside a gate script that imports nothing, so the path must go through it into src/ rather than end on the gate. It holds no test and is never meant to run."
    },
    {
      "path": "fixtures/atlas/start-order/checks-too/test/cli.test.js",
      "reason": "Atlas fixture, not a test. It sits inside the start-order fixture repository that packages/atlas/adapter/page-start-order.test.js maps, where CI's node --test runs it beside a lint of src/, so the page must say CI runs only tests and checks. It holds no test and is never meant to run."
    },
    {
      "path": "fixtures/atlas/turbo-runs/packages/core/test/shape.test.js",
      "reason": "Atlas fixture, not a test. It sits inside the turbo-runs fixture repository that packages/atlas/core/turbo-runs.test.js maps, where turbo runs the core member's test script, node --test test/, so CI must count it among the files it runs. It holds no test and is never meant to run."
    },
    {
      "path": "fixtures/atlas/vitest-projects/packages/a/src/a.test.ts",
      "reason": "Atlas fixture, not a test. It sits inside the vitest-projects fixture repository that packages/atlas/core/vitest-projects.test.js maps, where the root vitest config hands the run to each package's own config or vitest's defaults, and whether this file is selected is the point. It holds no test and is never meant to run."
    },
    {
      "path": "fixtures/atlas/vitest-projects/packages/b/test/b.test.ts",
      "reason": "Atlas fixture, not a test. It sits inside the vitest-projects fixture repository that packages/atlas/core/vitest-projects.test.js maps, where the root vitest config hands the run to each package's own config or vitest's defaults, and whether this file is selected is the point. It holds no test and is never meant to run."
    },
    {
      "path": "fixtures/atlas/test-spawn-joined/tests/search.test.mjs",
      "reason": "Atlas fixture, not a test. It sits inside the test-spawn-joined fixture repository that packages/atlas/core/test-spawn-joined.test.js maps, where it runs scripts/query.mjs through a command line held in a const, so the map must read that spawn. It holds no test and is never meant to run."
    },
    {
      "path": "fixtures/atlas/private-command/test/cli.test.js",
      "reason": "Atlas fixture, not a test. It sits inside the private-command fixture repository that packages/atlas/adapter/page-private-command.test.js maps, where CI's node --test runs it, so the path follows the command a private package declares. It holds no test and is never meant to run."
    }
  ]
}
